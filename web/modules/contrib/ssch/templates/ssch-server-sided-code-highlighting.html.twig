{#
/**
 * @file
 * Default theme implementation for a server sided highlighting of a code snippet.
 *
 * Available variables:
 * - highlighted:
 *   - code: HTML generated by the code highlighting library.
 *   - language: the programming language as determined by the highlighting library.
 * - code: the original code fragment.
 * - language: the programming language the code snippet is written in.
 * - lines_limit: the number of code lines that should be shown (for displaying a trimmed version).
 * - ellipsis: whether or not '...' are replaced by the ellipsis symbol.
 * - style: the stylesheet of the highlight.php package to apply.
 * - display_language: indicates whether the programming language should be printed.
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_ssch_server_sided_code_highlighting()
 *
 * @ingroup themeable
 */
#}
{%
  set classes = [
    'hljs',
    'hljs-' ~ highlighted.language,
  ]
%}
{% block content %}
  {% if display_language %}
    <div>
      {% trans %}The programming language is{% endtrans %}: <strong>{{ highlighted.language }}</strong>
    </div>
  {% endif %}

  <pre><code{{ attributes.addClass(classes) }}>{{ highlighted.code|raw }}</code></pre>
{% endblock %}
