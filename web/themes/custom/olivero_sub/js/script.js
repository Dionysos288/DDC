/**
 * @file
 * Custom scripts for Olivero Sub theme.
 */
(function ($, Drupal) {
  "use strict";

  Drupal.behaviors.oliveroSub = {
    attach: function (context, settings) {},
  };
})(jQuery, Drupal);
